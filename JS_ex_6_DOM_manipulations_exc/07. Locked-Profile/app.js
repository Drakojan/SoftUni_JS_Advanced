function lockedProfile() {
    
    let screen = document.getElementsByTagName('main')[0];
document.querySelectorAll
    screen.addEventListener('click',(e)=>{
        let parent = e.target.parentElement;

        console.log(parent.querySelectorAll(#div);
        
        switch (e.target.parentElement.getElementsByTagName('input')[2].value) {
            case 'User 1 Userov':
                e.target.parentElement.getElementsByTagName('input')[3].style.display = 'block';
                e.target.parentElement.getElementsByTagName('input')[4].style.display = 'block';
                break;
        
            default:
                break;
        }

    })
}